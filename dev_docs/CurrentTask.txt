NETWATCH CURRENT TASK
=====================
Updated: 2026-03-26 | Session 33 closed | v3.3.97

STATUS: Session 33 complete and closed out.

SESSION 33 SUMMARY
------------------
Two packages shipped:

v3.3.96 — Update Checker
  Monitor service polls releases/latest.json daily (with 0-60min jitter).
  system_settings DB table for inter-service state.
  Dismissible notification banner in base.html (Admin + Operator).
  Always shown on /admin/patch regardless of dismiss state.
  UPDATE_CHECK_URL config key. system_update alert type added.
  MIGRATIONS.md created at repo root (empty shell).

v3.3.97 — Update Installer
  parse_migrations() + apply_migrations() in patcher.py.
  Skip-version upgrades supported (all intermediate SQL applied in order).
  /api/update/download — fetches zip, verifies SHA-256, stores locally.
  /api/update/install — applies migrations then patcher. Admin only.
  Two-step UI in patch_manager.html with restart polling + rollback notice.

NEXT SESSION
------------
Primary candidates (no order decided — Matt to choose):

  1. Security audit
     Review attack surface before any public promotion:
     - Package installer / deployed files uploader (path traversal, code execution)
     - Auth / session hardening
     - Physical and remote access review
     Goal: confidence before distributing to others.

  2. Distributable installer design brief
     PC-side application (netwatch-setup.exe concept):
     - Handles Pi imaging, SD card pre-configuration
     - SSH-based install initiation
     - No SSH knowledge required from user
     Design brief required before any code.

  3. Documentation
     User PDF, Admin guide PDF, Developer reference, Changelog PDF.

  4. mBase / architecture pending discussion items
     9 PENDING DISCUSSION items in Audit_Findings.txt still unresolved.
     Load all four mBase docs at session start if this is the chosen task.

NOTE
----
MIGRATIONS.md rule going forward: whenever a session package includes a
run_sql action, that SQL also goes into MIGRATIONS.md under the new version
heading. This is now part of the development discipline.

releases/ directory not yet in install.sh. Low priority — update checker
creates updates/ dir at download time. Revisit when distributable installer
is built.
