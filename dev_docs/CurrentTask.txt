NETWATCH CURRENT TASK
=====================
Updated: 2026-03-27 | Session 37 closed | v3.4.40 (production)

STATUS: Session 37 complete and closed out.

SESSION 37 SUMMARY
------------------
Six features across 7 packages:
- v3.4.34: Release Manager field locking + Reset button + description in zip manifest
- v3.4.35: MFA_REQUIRED config key + Security card on Admin page
- v3.4.36: SUMMARY_FREQUENCY / SUMMARY_DAY / SUMMARY_HOUR config keys
- v3.4.37-38: Config Editor allowed_values dropdowns + HTML bold descriptions
- v3.4.39-40: Summary Settings card on Notifications page + Send Test Summary

KEY STATE
---------
- Production Pi: v3.4.40
- Test Pi: v3.4.33 (needs publish before it can update)
- GitHub: v3.4.38 is current public release
- New config keys added this session (auto-added by validator on startup):
    MFA_REQUIRED, SUMMARY_FREQUENCY, SUMMARY_DAY, SUMMARY_HOUR,
    SUMMARY_SHOW_UPTIME, SUMMARY_SHOW_RESETS, SUMMARY_SHOW_LATENCY,
    SUMMARY_SHOW_SPEEDTEST, SUMMARY_SHOW_RESET_LOG
- save_quiet route pattern: POST /api/config/save_quiet — saves config without
  service restart. Currently used for SUMMARY_SHOW_* toggles. Reusable pattern.

NEXT SESSION
------------
Priority order:

  1. Publish v3.4.40 to GitHub
     Test Pi at v3.4.33 needs this to update via update checker.

  2. Security audit
     Attack surface review before public promotion. Package installer,
     auth/session, physical/remote access hardening.

  3. Documentation
     User PDF, Admin guide, Developer reference.

  4. Version reset to v1.0.0
     After above. Clear MIGRATIONS.md of 3.x.x entries when cutting v1.0.0.

NOTES
-----
- alerts.py is never imported at module level in webapp.py. Always use
  local "import alerts" inside any route function that needs it.
- allowed_values in config_schema.py triggers dropdowns in both Config Editor
  modal and main editor. Reusable pattern for future constrained string keys.
- save_quiet is appropriate only for runtime-read keys (not startup-read ones).
