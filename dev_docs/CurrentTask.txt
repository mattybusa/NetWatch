NETWATCH CURRENT TASK
=====================
Updated: 2026-03-27 | Session 35 closed | v3.4.18 (production)

STATUS: Session 35 complete and closed out.

SESSION 35 SUMMARY
------------------
Release pipeline validated (v3.4.9). Fresh install testing revealed cascade of
failures — fixed across v3.4.10–v3.4.16. Major install.sh improvements: dynamic
package resolution, self-contained curl install, GitHub sync fix, re-run resilience.
Setup wizard: interface auto-detection fields added (v3.4.17), simple network WiFi
monitoring fix (v3.4.18).

KEY STATE
---------
- Production Pi: v3.4.18
- Test Pi: v3.4.16 (last fresh install — v3.4.17/18 not yet installed)
- GitHub: v3.4.16 is current public release
- v3.4.18 installed on production but NOT yet published to GitHub

NEXT SESSION
------------
Priority order:

  1. Publish v3.4.18 to GitHub as public release
     Do this first so test Pi can pick it up and fresh install test uses
     current code.

  2. Fresh install re-test with v3.4.18
     Wizard changes (interface auto-detection, WiFi simple network fix)
     not verified on a clean Pi. Run curl install, go through wizard,
     confirm eth0/wlan0 pre-populated, confirm config.py has correct values.

  3. Release description auto-generation
     Discussed in Session 35 — pull package descriptions from changelog
     since last published release, concatenate, pre-populate Release Manager
     description field. Straightforward to implement.

  4. Security audit
     Attack surface review before public promotion.

  5. Documentation
     User PDF, Admin guide, Developer reference.

  6. Version reset to v1.0.0
     After above. Clear MIGRATIONS.md of 3.x.x entries when cutting v1.0.0.

NOTE
----
MIGRATIONS.md rule: whenever a session package includes a run_sql action,
add the same SQL to MIGRATIONS.md under the new version heading before closeout.
No run_sql actions this session — MIGRATIONS.md unchanged.
