NETWATCH CURRENT TASK
=====================
Updated: 2026-03-23 | Session 29 closed | v3.3.89

STATUS: Session 29 complete and closed out.

SESSION 29 SUMMARY
------------------
- v3.3.88: MFA verification codes redacted from alert history. _redact_sensitive()
  in database.py now strips both email-format and SMS-format codes at write time.
- v3.3.89: Role-based alert permissions. New Alerts tab in Users & Roles with
  Available/Email/SMS controls per alert type per role. Standalone subscriber
  defaults via sentinel role_id=0. My Account now data-driven instead of hardcoded
  ALLOWED_GROUPS. Grid compressed vertically.

NEXT SESSION
------------
Open candidates:
  1. Security audit (attack surface review -- package installer, auth/session hardening)
  2. Kiosk display (operational quick-glance status view, scope narrowed)
  3. Package storage + distribution story (design together with distributable
     installer, update package installer, git strategy -- cohesive design unit)

See NetWatch_AI_Context_latest.txt backlog section for full list.

POST-INSTALL NOTE
-----------------
After installing v3.3.89, all role_alert_defaults rows will have available=1 (the
migration default). Configure the Alerts tab to restrict types per role as desired --
nothing changes for existing users until you explicitly save.
