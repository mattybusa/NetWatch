NETWATCH CURRENT TASK
=====================
Updated: 2026-03-13 | Session 11 complete | v3.3.29

STATUS: Session 11 complete. Pre-mBase audit done. Gitea infrastructure live.
Next: Build git webhook auto-deployment feature in NetWatch.

LAST COMPLETED (Session 11)
----------------------------
- Pre-mBase audit completed. Output: dev_docs/Audit_Findings.txt
- Gitea Pi set up from scratch (Pi 3B+, Samsung 860 EVO 500GB SSD)
  Static IP: 192.168.100.230 | URL: https://gitea-pi.home:3000
- NetWatch codebase pushed to Gitea (132 files, initial commit v3.3.29)
- mbase repo created in Gitea (empty, ready for extraction work)
- Development workflow designed: full automated pipeline planned
- CONFIRMED: patcher.py temp file backlog item already fixed (v3.3.26-3.3.29)
- CONFIRMED: NetWatch CA key has no passphrase, certs valid until 2036
- CONFIRMED: theme_manager.py PermissionError bug is fixed

NEXT SESSION: GIT WEBHOOK AUTO-DEPLOYMENT
------------------------------------------
Build git-push-triggered auto-deployment for NetWatch.
This replaces manual Package Installer uploads with an automated pipeline.
Package Installer remains as manual fallback.

Design-first required (per AI_Session_Process.txt v1.4). Before writing code,
produce a design brief covering:
- Webhook receiver endpoint (/api/webhook/gitea)
- Gitea HMAC-SHA256 signature verification
- git pull integration
- How existing patcher logic integrates (syntax check, snapshot, restart)
- Alert on success/failure
- Rollback on failed deployment

Build order (suggested):
1. Webhook receiver + signature verification (safe, no deployment yet)
2. git pull on verified webhook
3. Patcher integration (syntax check + snapshot + apply)
4. Service restart
5. Alert notification
6. Test end-to-end: push to Gitea -> NetWatch auto-deploys

Once working in NetWatch, this becomes a mBase core feature.

ALSO NEXT SESSION (lower priority):
- SESSION_COOKIE_SECURE = True fix (one line, one package -- easy win)
- Review Audit_Findings.txt PENDING DISCUSSION items with Matt
- mBase extraction planning once audit decisions are finalized

GITEA INFRASTRUCTURE (reference)
----------------------------------
Gitea Pi: 192.168.100.230 | https://gitea-pi.home:3000
Repos: mboynton/netwatch, mboynton/mbase (both private, MIT license)
Remote on NetWatch Pi: https://gitea-pi.home:3000/mboynton/netwatch.git
Push command: cd ~/netwatch && git add . && git commit -m "message" && git push
Credentials: stored via git credential.helper store (passwordless)
NetWatch Pi hosts file: 192.168.100.230 gitea-pi.home
NetWatch Pi CA trust: netwatch-ca.crt installed system-wide

GITEA SSH NOTE
--------------
Built-in SSH server on port 2222 not successfully started. Deferred.
HTTPS with stored credentials is fully functional -- no action needed.
Investigate SSH in a future session if desired.

WIREGUARD VPN
-------------
Planned for remote development access (travelling use case).
Deferred until after webhook feature is built.
Run on router if supported, else on a dedicated Pi.

DESIGN-FIRST NORM (v1.4)
--------------------------
Before writing any code for a new feature, Claude must produce a design brief:
- The meaningful approaches (2-4 options)
- What each costs and buys in plain language
- A clear recommendation with reasoning
Wait for explicit user approval before writing code.
See AI_Session_Process.txt v1.4 for full details.

RESOLVED THIS SESSION
----------------------
- theme_manager.py PermissionError -- CONFIRMED FIXED. Remove from tracking.
- Patcher temp file handling -- CONFIRMED ALREADY DONE. Remove from backlog.

PENDING VERIFICATION
--------------------
- Weekly Pi update cron (v3.3.12): not yet confirmed fired in the wild.
  Verify: crontab -l | grep netwatch-pkg-update (run as netwatch-svc)
- Web service silent-exit after delayed restart: unconfirmed potential bug.

OPEN ITEMS
----------
- Orphan test keys (TEST_KEY_WITH_DEFAULT, TEST_KEY_UNCONFIGURED) may remain
  in config.py. Harmless -- validator ignores unknown keys.
- System log viewer for pkg_update.log in Admin UI (backlog)
- Host monitoring (longer-term, first mBase module candidate)
- SSD migration on NetWatch Pi (Samsung 860 EVO available)
- mBase audit decisions: see Audit_Findings.txt PENDING DISCUSSION section
