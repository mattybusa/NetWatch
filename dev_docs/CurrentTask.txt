NETWATCH CURRENT TASK
=====================
Updated: 2026-03-29 | Session 40 closed | v3.4.47

STATUS: Session 40 complete. Security audit finished. Hardware fully operational
  for the first time. See ChatSummary_40.txt for full detail.


SECURITY AUDIT — COMPLETE
--------------------------
All items resolved, accepted, or formally deferred:

SHIPPED THIS SESSION (v3.4.44):
  C2 ✅ DB restore schema validation
  C3 ✅ Destructive SQL guard + auto-reset (ALLOW_DESTRUCTIVE_SQL config key)
  H3b ✅ Reset token replay block
  M4 ✅ Config export credential warning

DISCUSSED AND CLOSED THIS SESSION:
  M4 ✅ Config export — already encrypted (.nwcfg), warning text added
  M5 ✅ Per-IP rate limiting — accepted/deferred (single-user, per-user lockout sufficient)
  C4 ✅ Package signing — CLOSED. SHA-256 on GitHub releases is sufficient.
         Revisit if distribution moves beyond GitHub.

STILL PENDING (low urgency):
  H4 — PAT scrubbing from patch_log error field


HARDWARE — NOW FULLY OPERATIONAL
----------------------------------
All hardware working as of Session 40:
- Relays: working via web UI and auto-trigger
- Button: working (lgpio, GPIO 22)
- Button mapping:
    Short press  → Full reset (modem + router)
    Long press   → Router only reset
    Triple press → Toggle lockout mode

Manual fixes applied this session (already in install.sh for fresh installs):
- sudo usermod -a -G gpio netwatch-svc (relay GPIO access)
- sudo /home/mboynton/netwatch/venv/bin/pip install lgpio (button lgpio)
- echo "192.168.100.230 ops-pi.home" >> /etc/hosts (Gitea DNS)


NEXT SESSION
------------
Strategic direction from Session 39 still applies:
1. H4 — PAT scrubbing (low urgency, can bundle with any small fixes)
2. mBase architecture session — load all four architecture docs, work through
   all PENDING DISCUSSION items in Audit_Findings.txt, resolve:
   - Flask-Security-Too integration approach
   - Phone-first transport decision (browser/PWA/native)
   - Module system design
   - Blueprint refactor approach
3. Test Pi update (v3.4.40 → v3.4.47)
4. Publish security release to GitHub (audit now complete)
5. Documentation (User PDF, Admin guide, Developer reference)
6. v1.0.0 version reset


KEY STATE
---------
- Production Pi: v3.4.47
- Test Pi: v3.4.40
- GitHub: v3.4.40 (hold — publish after test Pi confirms update path)
- Gitea: v3.4.47 (pushed manually after DNS fix)
- All security audit items resolved
- Hardware fully operational
