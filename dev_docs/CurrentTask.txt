NETWATCH CURRENT TASK
=====================
Updated: 2026-03-25 | Session 32 closed | v3.3.95

STATUS: Session 32 complete and closed out.

SESSION 32 SUMMARY
------------------
GitHub infrastructure set up:
- Account: mattybusa
- Public repo: github.com/mattybusa/NetWatch
- PAT: netwatch-pi (repo-scoped, Contents read+write, no expiration)
- github remote added to Pi (PAT embedded in URL, same pattern as Gitea)
- Full codebase + history pushed
- releases/latest.json placeholder committed to both remotes
- Manifest URL confirmed live:
  https://raw.githubusercontent.com/mattybusa/NetWatch/main/releases/latest.json
- Release workflow: manual push to github remote when ready to publish

NEXT SESSION
------------
Primary candidate:
  Update checker — monitor service polls releases/latest.json daily (with jitter),
  compares version to installed version, writes available version to DB,
  web service reads DB and shows notification banner to Admin/Operator users.

  Design decisions already made (Session 31):
  - Model C: user-initiated update with notification, no auto-install by default
  - UPDATE_CHECK_URL config key (defaults to manifest URL, blank = disabled)
  - First-run wizard asks opt-in question
  - system_update alert type (subscribable, Admin only by default)
  - Notification banner visible to Admin + Operator (manage_admin permission)
  - Only Admin can install (manage_users permission)
  - Fails silently on network errors

  Work required:
  - New config key: UPDATE_CHECK_URL (config_schema.py)
  - Monitor service: daily poll with jitter, version comparison, DB write
  - New DB table or user_pref for available_version
  - webapp.py: API endpoint to read available version
  - Notification banner in base.html or dashboard
  - Design brief still required before code (new feature norm)

Other open candidates:
  - Update installer (download + sha256 + patcher) — follows update checker
  - Security audit (pre-distribution)
  - Distributable installer design brief (PC-side netwatch-setup.exe)
  - Documentation (User PDF, Admin guide)

NOTE
----
releases/ directory not in install.sh yet. Low priority — update checker can
create it on first run, or add to install.sh when update checker is built.
